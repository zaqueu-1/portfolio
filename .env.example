# Sem query string — o sync adiciona ?locale=pt_BR / ?locale=en-US
LINKEDIN_PROFILE_URL=https://www.linkedin.com/in/zaqueu1/
# Contact form (api/contact.ts). Server-side only, never prefix with VITE_.
RESEND_API_KEY=
# Optional overrides. FROM must be a Resend-verified domain to send to anyone
# other than the Resend account owner; the default onboarding sender works for that owner.
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=
